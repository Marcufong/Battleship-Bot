"""Battleship bot: imitation-CNN + RL-tuned targeting policy."""

__version__ = "0.1.0"


def main() -> None:
    """CLI entry point for the `battleship-bot` script.

    Package is still scaffolding; wire real entry logic here once
    src/gen/, src/solver/, src/model/, src/prune/, src/rl/, src/eval/
    have implementations.
    """
    print("battleship-bot: hello from battleship_bot package")
